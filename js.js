let phoneNumbers = ''

// Split the string into an array of numbers
let numbersArray = phoneNumbers.split(',');

// Join the array into a string with line breaks
let formattedNumbers = numbersArray.join('\n');

console.log(formattedNumbers);

